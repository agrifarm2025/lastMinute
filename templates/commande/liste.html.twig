{# templates/commande/liste_commandes.html.twig #}

{% extends 'base_front.html.twig' %}

{% block body %}
<div class="container mt-5">
    <h1 class="text-center mb-4">📦 Mes Commandes</h1>

    <div class="table-responsive shadow-sm p-3 mb-5 bg-white rounded">
        <table class="table table-hover text-center align-middle">
            <thead class="table-dark">
                <tr>
                    <th>ID</th>
                    <th>Date</th>
                    <th>Total</th>
                    <th>Statut</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
                {% for commande in commandes %}
                    <tr class="fw-semibold">
                        <td>#{{ commande.id }}</td>
                        <td>
                            {{ commande.DateCreationCommande ? commande.DateCreationCommande|date('d/m/Y H:i') : 'Non définie' }}
                        </td>
                        <td class="text-success">{{ commande.prix }} €</td>
                        <td>
                            <span class="badge {% if commande.status == 'En attente' %} bg-warning text-dark 
                                {% elseif commande.status == 'Confirmée' %} bg-success 
                                {% elseif commande.status == 'Annulée' %} bg-danger 
                                {% else %} bg-secondary {% endif %}">
                                {{ commande.status }}
                            </span>
                        </td>
                        <td>
                            <a href="{{ path('generer_facture', {'id': commande.id}) }}" class="btn btn-outline-primary btn-sm">
                                📄 Télécharger
                            </a>
                        </td>
                    </tr>
                {% else %}
                    <tr>
                        <td colspan="5" class="text-muted py-4">Aucune commande trouvée</td>
                    </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endblock %}
